pipeline{
    agent any

    options {
        buildDiscorder(logRotator(numToKeepStr: '2'))
        disableConcurrentBuilds()
        disableResume()
        retry(3)
        timeout(time: 1, unit: 'MINUTES')
    }

    stages {
        stage('Stage1'){
            steps {
                
                sh "sleep 30"
            }
        }

        stage('Stage2'){
            steps{
                sh "sleep 50"
            }
        }
    }

    
}