pipeline {
    agent none

    stages {
        stage('Master Node'){
            agent {label 'Master'}

            steps {
                echo "checking pwd"
                sh "pwd"
            }
            
        }
    }
}