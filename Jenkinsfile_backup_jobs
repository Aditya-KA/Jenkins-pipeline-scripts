pipeline {
    agent none

    stages {
        stage('Master Node'){
            agent {label 'Master'}

            steps {
                echo ""
                sh ''' cd /var/lib/jenkins/jobs
                ls -lrt
                sudo cp -rf /var/lib/jenkins/jobs /Jobs_backup/jobs_$(date +%d-%m-%y"_"%H:%M:%S) '''
            }
            
        }
    }
}
