pipeline {
    agent none

    stages {
        stage('stage1') {
            agent { label 'Slave_2' }

            steps {
                echo "I'm running slave1 agent"
            }
        }

        stage('stage2') {
            steps {
                echo "I'm running slave2 agent"
            }
        }
    }
}